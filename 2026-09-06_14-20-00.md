# Update: Blog YAML Frontmatter & Automation Skill
**Date**: 2026-09-06

## Changes Made
1. **YAML Frontmatter Integration**: 
   - Upgraded the `script.js` markdown parser to intelligently extract standard YAML frontmatter blocks (delimited by `---`) at the top of `.md` blog files.
   - It now extracts custom metadata, automatically injecting `read_time`, `author`, `date`, and overriding the `title` directly into the DOM (`post.html`).
   - Retroactively updated the `hello-world.md` sample post to utilize this new metadata schema.
2. **AI Skill Definition (`blog-writer`)**: 
   - Authored `.gemini/skills/blog-writer/SKILL.md` to permanently train the AI agent on how to write blogs for Sphita.
   - This skill documents the exact workflow: creating the `.md` file, inserting the YAML block, registering the post in `blog-index.js`, logging the update, and pushing to GitHub.
