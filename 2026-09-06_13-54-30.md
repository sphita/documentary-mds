# Update: JavaScript Lock Animation
**Date**: 2026-09-06

## Changes Made
1. **HTML Updates**: Modified the "Open-First Approach" card in `index.html` to hold both the closed lock (`lock_24dp_...`) and the open lock (`lock_open_24dp_...`) positioned absolutely over one another.
2. **JS Animation Injection**: In `script.js`, added event listeners (`mouseenter` / `mouseleave`) to the feature card that programmatically manipulates inline CSS styles. It smoothly crossfades the closed lock out and dynamically animates the open lock in, scaling it up and adding the blue drop shadow entirely via JavaScript.
