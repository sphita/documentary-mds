# Update: Dynamic Per-Dataset Pages & Exact Naming
**Date**: 2026-09-06

## Changes Made
1. **Homepage Accuracy**: Updated the Featured Datasets grid on `index.html` to display exact repository names (e.g., `AdhyanshVerma/stack-2021-12-01`) and exact descriptions pulled directly from the Hugging Face API.
2. **`dataset.html` Created**: Engineered a new dynamic per-dataset view. This page reads URL parameters (`?id=...`), fetches live metadata badges (Downloads, Likes, Last Modified) from HF, and dynamically downloads, cleans, and renders the dataset's `README.md` file using `marked.js`.
3. **Routing Script Cleanup**: Rewrote and optimized `script.js` to handle both the blog routing and the new dataset routing without conflict.
